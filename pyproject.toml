[project]
name = "tokcomp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Antareep Roy", email = "antareep_23ucs083@ug.nita.ac.in" }
]
requires-python = ">=3.12"
dependencies = [
    "nltk>=3.10.3",
    "pandas>=3.0.5",
    "tiktoken>=0.14.0",
    "transformers>=5.15.1",
]

[project.scripts]
tokcomp = "tokcomp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
